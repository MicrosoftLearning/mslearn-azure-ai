apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  labels:
    app: aks-config-api
data:
  # Non-sensitive configuration values
  STUDENT_NAME: "YourNameHere"  # Students should update this with their name
  API_VERSION: "1.0.0"
  LOG_PATH: "/var/log/api"
