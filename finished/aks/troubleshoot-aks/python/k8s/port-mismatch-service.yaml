apiVersion: v1
kind: Service
metadata:
  name: api-service
  labels:
    app: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
  - name: http
    port: 80
    # ERROR: targetPort set to 9000, but container listens on 8000
    targetPort: 9000
    protocol: TCP
